server:
  port: 8887
spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/pwhuanco/config_data.git
          search-paths: config-data
          default-label: "main"

